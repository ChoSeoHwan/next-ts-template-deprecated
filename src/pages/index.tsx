import * as React from 'react';

import Layout from 'templates/Layout';

const Home = () => {
    return (
        <Layout title={'Home'}>
            <h1>Hello, Next Ts</h1>
        </Layout>
    );
};

export default Home;
